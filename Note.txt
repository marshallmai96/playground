brew install git
brew upgrade git

git config --global user.name "Marshall"
git config --global user.email "marshallmai@gmail.com"

brew install gh
brew upgrade git
gh auth login

cd ~/Desktop
git clone https://github.com/marshallmai96/playground.git

git add .
git commit -m "Test"
git push origin main

git pull origin main
git fetch origin
git merge origin/main

brew install python@3.11
python3.11 --version